# Compatibility patch for upstream commit 950c06dc8dae360ebe701bf00ff51542ac08c2b2.
# It contains upstream context and is not covered by this suite's MIT license.
diff --git a/hfss_server.py b/hfss_server.py
index a726686..e8fb92a 100644
--- a/hfss_server.py
+++ b/hfss_server.py
@@ -11,8 +11,10 @@ Requirements:
     - mcp (Model Context Protocol)
 """
 
-# 禁用 atexit.register 防止 PyAEDT 注册退出回调（在导入 ansys 之前）
+# 仅在导入 PyAEDT 时拦截其退出回调，导入后立即恢复全局注册函数。
 import atexit
+import sys
+from contextlib import redirect_stdout
 _original_register = atexit.register
 
 def _no_op_register(*args, **kwargs):
@@ -24,7 +26,13 @@ atexit.register = _no_op_register
 from mcp.server import Server
 from mcp.server.stdio import stdio_server
 from mcp.types import Tool, Resource, TextContent
-from ansys.aedt.core import Hfss
+try:
+    # PyAEDT 导入阶段会把版本信息写到 stdout；重定向到 stderr，避免破坏 MCP stdio。
+    with redirect_stdout(sys.stderr):
+        from ansys.aedt.core import Hfss
+        from ansys.aedt.core.generic.settings import settings as pyaedt_settings
+finally:
+    atexit.register = _original_register
 import logging
 import asyncio
 from typing import Optional, Dict, Any, List
@@ -45,6 +53,12 @@ logger = logging.getLogger(__name__)
 # Initialize the MCP server
 app = Server("HfssMcp")
 
+# 本机安装版本：Ansys Electronics Desktop 2023 R1。
+AEDT_VERSION = "2023.1"
+
+# stdio 仅能输出 MCP JSON，PyAEDT 运行日志不得混入 stdout。
+pyaedt_settings.enable_screen_logs = False
+
 
 @dataclass
 class ProjectSession:
@@ -126,6 +140,7 @@ class HfssSessionManager:
             hfss_app = Hfss(
                 project=None,
                 design=None,
+                version=AEDT_VERSION,
                 new_desktop=False,
                 close_on_exit=False
             )
@@ -231,6 +246,7 @@ class HfssSessionManager:
                 project=project_name,
                 design=design_name,
                 solution_type=solution_type,
+                version=AEDT_VERSION,
                 new_desktop=False,
                 close_on_exit=False
             )
@@ -258,7 +274,7 @@ class HfssSessionManager:
             logger.error(f"Failed to create session: {e}")
             raise
     
-    def close_session(self, release_resources: bool = True) -> bool:
+    def close_session(self, release_resources: bool = False) -> bool:
         """关闭 session"""
         if not self._current_session:
             logger.info("No active session to close")
@@ -563,7 +579,7 @@ def get_tool_definitions() -> List[Tool]:
         ),
         Tool(
             name="hfss_stop_app",
-            description="Stop/Close HFSS application and release resources",
+            description="Detach the MCP session without closing AEDT",
             inputSchema={
                 "type": "object",
                 "properties": {
@@ -573,7 +589,7 @@ def get_tool_definitions() -> List[Tool]:
         ),
         Tool(
             name="hfss_restart_app",
-            description="Restart HFSS application",
+            description="Disabled for safety; restart AEDT manually",
             inputSchema={
                 "type": "object",
                 "properties": {
@@ -680,7 +696,7 @@ def get_tool_definitions() -> List[Tool]:
         ),
         Tool(
             name="hfss_run_analysis",
-            description="Run simulation analysis",
+            description="Disabled for safety in this local integration",
             inputSchema={
                 "type": "object",
                 "properties": {
@@ -958,29 +974,14 @@ async def handle_tool_call(name: str, arguments: Dict[str, Any]) -> str:
             return error("Failed to connect to HFSS")
         
         elif name == "hfss_stop_app":
-            force = arguments.get("force", False)
-            
             if not session_manager.is_valid():
                 return error("No active HFSS connection")
-            
-            session_manager.close_session(release_resources=True)
-            
-            if force:
-                for proc in psutil.process_iter(['pid', 'name']):
-                    try:
-                        if 'ansys' in proc.info['name'].lower() or 'hfss' in proc.info['name'].lower():
-                            psutil.Process(proc.info['pid']).kill()
-                    except:
-                        pass
-                return success("HFSS forcefully stopped")
-            return success("HFSS stopped and resources released")
+            # 只断开 MCP 会话，不释放桌面、不结束任何 AEDT 进程。
+            session_manager.close_session(release_resources=False)
+            return success("HFSS MCP session detached; AEDT remains open")
         
         elif name == "hfss_restart_app":
-            session_manager.close_session(release_resources=True)
-            
-            if session_manager.init_session():
-                return success(f"HFSS restarted")
-            return error("Failed to restart HFSS")
+            return error("Disabled for safety. Restart AEDT manually, then call hfss_start_app")
         
         elif name == "hfss_launch_app":
             non_graphical = arguments.get("non_graphical", False)
@@ -993,6 +994,7 @@ async def handle_tool_call(name: str, arguments: Dict[str, Any]) -> str:
                 hfss_app = Hfss(
                     project=None,
                     design=None,
+                    version=AEDT_VERSION,
                     new_desktop=True,
                     non_graphical=non_graphical,
                     close_on_exit=False
@@ -1142,15 +1144,7 @@ async def handle_tool_call(name: str, arguments: Dict[str, Any]) -> str:
 
 
         elif name == "hfss_run_analysis":
-            hfss = ensure_connection()
-            if not hfss:
-                return error("No active project")
-            setup_name = arguments["setup_name"]
-            try:
-                hfss.analyze_setup(setup_name)
-                return success(f"Analysis started for setup '{setup_name}'")
-            except Exception as e:
-                return error(f"Failed to start analysis: {e}")
+            return error("Disabled for safety. Run analysis manually in AEDT after reviewing the setup")
 
 
         elif name == "hfss_get_s_parameters":
diff --git a/requirements.txt b/requirements.txt
index c89028d..f05e85d 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,4 +1,5 @@
 mcp
-pyaedt
+pyaedt==0.25.1
 psutil
 pypdf
+pandas
